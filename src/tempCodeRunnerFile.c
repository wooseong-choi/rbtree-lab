
    // printf("작동1");
    // rbtree_to_array(t, res, n);
    // printf("작동이후");
    // for (int i = 0; i < n; i++) {
    //     (entries[i] == res[i])?printf("1"):printf("0");
    // }
    // free(res);